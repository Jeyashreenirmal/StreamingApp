apiVersion: v1
kind: Service
metadata:
  name: chatservice
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.type }}
  selector:
    component: chatservice
  ports:
    - protocol: TCP
      port: 3004         # cluster port
      targetPort: 3004 # container port React is listening on
